// Coordinates are 0 index based on both x and y
// Layout:      ITEM_NAME{ X_COORD, Y_COORD, SMALL_CHARACTER, ATTACK_STAT, DEFENSE_STAT, SPEED_STAT }

// TODO (Juan): We could add to this if we want
// TODO (Juan): NOTE - If we update params here, we have to update them also in the Map class

ARMOR{ 14, 2 }
WEAPON{ 13, 12 }